# Create a jenkins job using basic authentication
- jenkins_job:
    config: "{{ lookup('file', 'templates/test.xml') }}"
    name: test
    password: admin
    url: http://localhost:8000
    user: admin

# Delete a jenkins job using the token
- jenkins_job:
    name: test
    token: adskflsklkflkdfjlsdlsdlsjld
    state: absent
    url: http://localhost:8080
    user: admin